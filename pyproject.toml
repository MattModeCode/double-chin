[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "double-chin"
version = "0.1.0"
description = "Double Chin: a local, standalone voice-cloning app for your own voice"
requires-python = ">=3.11,<3.13"
dependencies = [
    "chatterbox-tts",
    "resemblyzer",
    "setuptools<81",
    "fastapi",
    "uvicorn",
    "python-multipart",
    "pywebview",
    "peft",
    "librosa",
]

[project.scripts]
double-chin = "double_chin.cli:main"

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: marks tests that require the real Chatterbox model or network access; run with DOUBLECHIN_E2E=1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/double_chin"]
