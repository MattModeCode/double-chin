.venv/
__pycache__/
*.egg-info/
.pytest_cache/
chinai_out.wav
.DS_Store

# PyInstaller build output — rebuildable, and contains files over GitHub's 100MB limit
build/
dist/

# Personal voice recordings — biometric data, never commit to the public repo
*.m4a

# Personal scripts the owner feeds the app — private content, never commit
voice clone scripts/

# Runtime data dir, relocated here from ~/.chinai on 2026-07-28 to keep all AI
# state under chin. Holds voice samples, a fine-tune checkpoint, and the style
# profile — biometric data and multi-MB binaries. Same reason as *.m4a above.
.chinai-data/
